# 发布审核参考

## 必需证据

- 对应集成 SHA 的完整验证报告（G7）当前有效。
- 迁移、配置、回滚、文档齐备。
- 无未解决阻断 Finding/Defect。

## 检查项

- 回滚方案可执行且已记录。
- 迁移顺序与兼容窗口。
- 配置变化是否与环境绑定。
- 凭据/生产数据不进入产物。
- 发布决策本身若是 `star: true`，必须有人工批准 `*`，Reviewer 只给证据结论，不代签人工授权。

## 输出

`RELEASE_REVIEW` 的 `APPROVE` 只证明"证据齐备、可进入合并"，不替代用户对合并/推送/部署的单独授权。
