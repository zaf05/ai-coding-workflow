schema_version: 1
name: "反例：凭据内联进定义"
workflow_id: "secret-inline-invalid"
error_code_mapping: {}
blocks:
  - label: deploy
    block_type: script
    role: implementer
    goal: "部署"
    complete_criterion: "必须被拒绝：secret 参数不得有内联值"
    parameters:
      - name: DB_PASSWORD
        type: secret
        value: "plaintext-should-never-be-here"
    commands:
      - {cmd: "echo deploy"}
    next_block_label: null
